kind: pipeline
spec:
  stages:
    - type: ci
      spect:
        steps:
          - name: pushes
            type: plugin
            spec:
              name: git-push-plugin
              spec:
                name: git-push
                inputs:
                  ssh_key: ${{ secrets.get("github-key") }}
            