kind: pipeline
spec:
  stages:
  - type: ci
    spec:
      steps:
      - name: Build
        type: run
        spec:
          container: mcr.microsoft.com/dotnet/sdk:8.0
          script: |-
            dotnet build