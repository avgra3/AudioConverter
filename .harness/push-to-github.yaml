kind: pipeline
spec:
  stages:
  - name: build
    spec:
      platform:
        arch: arm64
        os: linux
      steps:
      - spec:
          container:
            image: alpine:3
          script:
          - git push --mirror https://github.com/avgra3/AudioConverter.git
        type: run
    type: ci
version: 1
